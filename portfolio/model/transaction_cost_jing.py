import pandas as pd


def calculate(old_weights: pd.Series, new_weights: pd.Series) -> float:
    """compute the transaction cost assume the portfolio value is $1"""
    # write code here
    return 1 * 0.05
